class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        n = len(nums)
        summ = (n*(n+1))//2
        sum2 = sum(nums)
        missing_num = summ - sum2
        return missing_num
